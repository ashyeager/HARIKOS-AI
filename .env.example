# Required for the contact form. Keep these values server-side only.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional inquiry email delivery.
RESEND_API_KEY=
RESEND_FROM_EMAIL=
ADMIN_EMAIL=

# Optional local server override (defaults to 3000).
PORT=3000
